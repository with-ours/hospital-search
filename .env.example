# Location Services API Configuration
# Get your API key from your location services provider

# Required: API key for location services (autocomplete, geocode, search, map styles)
NEXT_PUBLIC_LOCATION_SERVICES_API_KEY=your_api_key_here

# Optional: Base URL for location services API
# Defaults to http://localhost:3001/location-services if not provided
NEXT_PUBLIC_LOCATION_SERVICES_BASE_URL=https://cdn.oursprivacy.com/location-services
